const express = require('express')
const router = express.Router()

// main router
// 보기 쉽게 ejs 를 이용해 확인하느라 생성 - 필요 없음
// router.get('/', (req,res) => {
//         res.render('main.ejs', {user:req.session})  
// })


module.exports = router